# File Organizer (Cross-Platform)

A lightweight, open-source desktop app to automatically organize your files by type.

## 🧩 Features
- Cross-platform (Windows, macOS, Linux)
- Real-time logs
- Custom folder mapping via JSON config
- Lightweight PySide6 UI

## 🚀 Run Locally
```bash
git clone https://github.com/yourname/file-organizer.git
cd file-organizer
pip install -r requirements.txt
python src/main.py
